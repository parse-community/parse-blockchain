{
  "name": "root",
  "private": true,
  "scripts": {
    "install": "husky install && lerna bootstrap",
    "test": "lerna run test --stream"
  },
  "devDependencies": {
    "husky": "6.0.0",
    "lerna": "^4.0.0"
  }
}
