{
  "name": "root",
  "private": true,
  "scripts": {
    "clean": "lerna clean && rm -r node_modules",
    "install": "husky install && lerna bootstrap",
    "test": "lerna run test --stream"
  },
  "devDependencies": {
    "husky": "6.0.0",
    "lerna": "^4.0.0"
  }
}
